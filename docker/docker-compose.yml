version: "3.9"

services:
  universal-rag:
    build: .
    container_name: universal-rag

    ports:
      - "8501:8501"

    env_file:
      - .env

    volumes:
      - ./storage:/app/storage
      - ./data:/app/data

    restart: unless-stopped